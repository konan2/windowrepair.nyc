import { Plugin } from 'rollup';

type ReloadConfig = {
    port: number;
    host: string;
};
declare const autoReload: (init?: Partial<ReloadConfig>) => Plugin;

export { type ReloadConfig, autoReload };
